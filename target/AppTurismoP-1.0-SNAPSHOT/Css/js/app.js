 meta-charset-utf-8 
//Animaciones Test
gsap.from(".bienvenido-a", {
    scrollTrigger: ".bienvenido-a", // start the animation when ".box" enters the viewport (once)
    opacity: 0,
    y: 100,
    duration: 1
});


gsap.from(".ayudamos-container", {
    scrollTrigger: ".ayudamos-container", // start the animation when ".box" enters the viewport (once)
    opacity: 0,
    y: 100,
    duration: 1
});


gsap.from("#img1", {
    scrollTrigger: "#img1", // start the animation when ".box" enters the viewport (once)
    opacity: 0,
    y: 100,
    duration: 1
});
gsap.from("#img2", {
    scrollTrigger: "#img2", // start the animation when ".box" enters the viewport (once)
    opacity: 0,
    y: 100,
    duration: 1
});
gsap.from("#img3", {
    scrollTrigger: "#img3", // start the animation when ".box" enters the viewport (once)
    opacity: 0,
    y: 100,
    duration: 1
});
gsap.from("#img4", {
    scrollTrigger: "#img4", // start the animation when ".box" enters the viewport (once)
    opacity: 0,
    y: 100,
    duration: 1
});
gsap.from("#img5", {
    scrollTrigger: "#img5", // start the animation when ".box" enters the viewport (once)
    opacity: 0,
    y: 100,
    duration: 1
});
gsap.from("#img6", {
    scrollTrigger: "#img6", // start the animation when ".box" enters the viewport (once)
    opacity: 0,
    y: 100,
    duration: 1
});



gsap.from(".escoje", {
    scrollTrigger: ".escoje", // start the animation when ".box" enters the viewport (once)
    opacity: 0,
    y: 100,
    duration: 1
});

gsap.from(".footer-contenido", {
    scrollTrigger: ".footer-contenido", // start the animation when ".box" enters the viewport (once)
    opacity: 0,
    y: 100,
    duration: 1
});